default:
  email_delivery:
    delivery_method: :smtp
    smtp_settings:
      address: "{{redmine.smtp.address}}"
      port: "{{redmine.smtp.address}}"
      authentication: :login
      domain: '{{redmine.smtp.domain}}'
      user_name: '{{redmine.smtp.user}}'
      password: '{{redmine.smtp.password}}'

  attachments_storage_path:
  autologin_cookie_name:
  autologin_cookie_path:
  autologin_cookie_secure:
  scm_subversion_command:
  scm_mercurial_command:
  scm_git_command:
  scm_cvs_command:
  scm_bazaar_command:
  scm_darcs_command:
  scm_subversion_path_regexp:
  scm_mercurial_path_regexp:
  scm_git_path_regexp:
  scm_cvs_path_regexp:
  scm_bazaar_path_regexp:
  scm_darcs_path_regexp:
  scm_filesystem_path_regexp:
  scm_stderr_log_file:
  database_cipher_key:
  rmagick_font_path:
production:
development:
